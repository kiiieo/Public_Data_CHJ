# Public_Data_CHJ
//원부이 결다조
--정규화(Normalization) -- 중복 데이터 제거 -- 엔티티증가(테이블 분할) --데이터 입력,수정,삭제 능력이 증가 --데이터 조회성능은 향상될 수 도있지만 아닐수도 있음
--1정규형(1NF : 1 Normalization form) : 1정규화과정
-- 모든 속성은 하나의 값만 가져야한다.(유사한 속성이 반복되는 경우도 포함)

2정규형(2NF) : 일반속성은 주식별자에만 종속해야함 (완전 함수적 종속) : 부분 함수적 종속 제거
pk중 일부분이 부분 함수적 종속성 , 결정자의 역할을 함, 완전 함수적 종속이 아님
부분 함수적 종속을 분리,제거 -> 완전 함수적 종속을 만듦
(pk가 복합키일때)

3정규형(3NF) : x->y, y->z, x->z 경우 - 이행 함수적 종속 제거
주식별자가 아닌 모든 속성간에는 서로 종속될 수 없다. 일반 속성이 다른 속성을 종속할때
(pk가 하나일때 대부분)

BCNF : 모든 결정자는 후보키어야 합니다. 거꾸로 일반 속성이 ->pk 일부의 결정자인 경우
후보키가 아닌 결정자를 제거
(pk가 복합키일때)

4정규형 

5정규형



//반정규화

(테이블 분할(수평분할, 수직분할)도 가능)
